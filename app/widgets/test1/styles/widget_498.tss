".Label": {
	color: '#000',
	font: {
		fontSize: 18,
		fontWeight: 'bold'
	},
	height: Ti.UI.SIZE,
	width: Ti.UI.SIZE
}

".Label1": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}

".Label2": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}

".Label3": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}

".Label4": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}

".Label5": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}

".Label6": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}

".Label7": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}

".Label8": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}

".Label9": {
    color: '#000',
    font: {
        fontSize: 18,
        fontWeight: 'bold'
    },
    height: Ti.UI.SIZE,
    width: Ti.UI.SIZE
}